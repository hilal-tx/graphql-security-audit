import type { Plugin } from '@envelop/core';
import type { GraphQLArmorConfig } from '@escape.tech/graphql-armor-types';
export declare const EnvelopArmorPlugin: <PluginContext extends Record<string, any> = {}>(config?: GraphQLArmorConfig) => Plugin<PluginContext>;
export declare class EnvelopArmor<PluginContext extends Record<string, any> = {}> {
    private readonly protections;
    constructor(config?: GraphQLArmorConfig);
    protect(): {
        plugins: Plugin<PluginContext>[];
    };
}
